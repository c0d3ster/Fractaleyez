# Fractaleyez Tasks

Instructions for agent: This file is the task inventory only. Workflow rules (branching, PRs, testing, archival, NEEDS HUMAN annotations) live in CLAUDE.md under "Overnight Agent Workflow". Work through Agent-Ready tasks in order. Do not attempt Decisions items; those require human input.

## Agent-Ready

- [ ] #3 [stack: user-platform] Create a `users` collection in Mongo keyed by `clerkId`. Clerk remains the source of truth for identity; this collection holds app data (settings, logo particle reference, display name cache, future entitlements). (stack order 3/7)
  - Creation: lazy-create on first authenticated request, plus Clerk `user.created`/`user.updated` webhook to sync display name and connected providers.
  - Investigate and document how the current Spotify and Google sign-ins surface display names in Clerk (native vs custom OAuth provider) and which field to cache.
  - Acceptance: signing in creates/fetches a users document with no duplicates under concurrent requests; display name synced; existing Clerk-only flows unaffected; a `/api/me` failure never blocks preset/pack loading.
  - Sub-tasks: (1) `server/models/User.ts` schema: `clerkId` (unique index, enforced at the DB level), `displayName`, `settings` (crossfade duration, `logoParticle` reference, HUD options), timestamps. `logoParticle` is the field the config-gear task writes and the `@user` resolver task reads. (2) `server/repositories/UserRepository.ts`, `server/services/UserService.ts` with `getOrCreateUser(clerkId)` implemented as a single atomic upsert (`findOneAndUpdate` with `upsert: true`), not a separate find-then-create, so concurrent first-requests can't race into duplicate documents. (3) `server/routes/meHandler.ts` + `api/me.ts`, mounted in `server/dev.ts`, called from `ConfigProvider.tsx` alongside the existing presets/packs fetch to lazy-create/fetch the user doc — this call must fail independently: a timeout or 5xx from `/api/me` must not block preset/pack data or the bundled-preset fallback, and existing Clerk-only flows must keep working with no user doc. (4) `server/routes/clerkWebhookHandler.ts` + `api/clerkWebhook.ts`, mounted in `server/dev.ts`, verifying Clerk's svix signature (new dep), handling `user.created`/`user.updated` via the same atomic upsert keyed on `clerkId` so a redelivered webhook event is a no-op rather than a duplicate or stale overwrite. (5) Investigation deliverable: Google is a Clerk-native provider exposing `first_name`/`last_name`/`image_url` directly; Spotify is not Clerk-native, so it's likely a custom OAuth provider surfacing data under `external_accounts[].username`/`public_metadata` instead — confirm against the actual Clerk dashboard config and document findings in the PR description.
  - NEEDS HUMAN: register the webhook endpoint URL in the Clerk dashboard, copy the signing secret into `CLERK_WEBHOOK_SECRET` (Vercel + local `.env`); confirm Spotify's exact provider configuration in the Clerk dashboard if it can't be determined from code alone.
- [ ] #4 [stack: user-platform] Add configuration gear for per-user settings, stored on the users collection. (stack order 4/7)
  - Settings to include: crossfade duration, user logo, HUD options (frequency visualization toggle).
  - Acceptance: settings persist per user and apply on load.
  - Sub-tasks: (1) Settings API (GET/PATCH) extending `/api/me` or a new `/api/userSettings` (if new, mount it in `server/dev.ts` alongside the existing routes), writing to the `users` collection's `logoParticle`/`settings` fields from the task above. (2) Gear icon + panel — candidate placement: `src/components/sidebar/Sidebar.tsx` near the "Configuration" row, or `src/components/topbar/TopBar.tsx` near `UserButton`. (3) User logo upload reuses the R2 upload endpoint from the R2 task (not the old inline `data:` URL path). (4) Load settings into `ConfigProvider.tsx` on sign-in; apply crossfade duration into the crossfade task's mechanism; wire the HUD toggle into `src/components/huds/FrequencyHud.tsx` (currently only a `window.enabledFreqBands` global, not persisted).
  - Missing acceptance criteria proposed: gear icon hidden/no-op when signed out (settings require a user doc); default to auto-save on change (debounced) since the task doesn't specify explicit Save-button UX — note this assumption in the PR.
- [ ] #5 [stack: solo] Add "red zone" warning in particle config when particle count surpasses a performance threshold.
  - Acceptance: exceeding the threshold shows a visible warning; threshold is a named constant, easy to tune.
  - Clarification: "particle count" should mean the computed total (`particlesPerLayer × layers`), which is what actually drives GPU load per the README's existing performance warning — not either slider read in isolation.
  - Sub-tasks: (1) Named threshold constant in `src/config/particle.config.ts`. (2) Live warning banner in the particle config UI (`src/components/config/ConfigCategory.tsx`/`ConfigWindow.tsx`) when the computed total exceeds it.
- [ ] #6 [stack: user-platform] Recreate the Shlump pack presets as `@user` variants: `@userSpin`, `@userSalad`, `Hyper@user`, and `@user`, each referencing the `@user` particle instead of a hardcoded artist particle. Copy the existing Shlump preset particle configs verbatim; they are already tuned and require no visual judgment or adjustment. One-off creation; do not modify or remove any existing presets or packs. (stack order 6/7 — execute after the `@user` resolver task below despite file order; this task's acceptance requires that resolver to exist)
  - Pack structure: create a premium pack named `@user` containing all four. Essentials additionally includes `@userSpin` for free.
  - Membership model: presets are standalone entities; packs hold references to preset IDs (many-to-many). `@userSpin` exists once and is referenced by both Essentials and the `@user` pack. If packs currently embed presets, refactor to references as part of this task. Entitlement: a user can load a preset if any pack they're entitled to includes it.
  - Acceptance: all four presets render per the `@user` fallback chain for any user; `@userSpin` loads for free users via Essentials; the other three require the premium pack; no preset data duplicated; all pre-existing packs untouched, meaning their visible content and behavior (name, presets shown, entitlement) is unchanged — the underlying storage move from embedded presets to `Pack.presetIds` references is additive (existing packs get `presetIds` populated, nothing existing is deleted), so no destructive migration or rollback path is needed.
  - Sub-tasks: (1) Many-to-many refactor: move membership onto `Pack.presetIds: Types.ObjectId[]` (not a `packId`/`packIds` field on `Preset` — the task text says "packs hold references to preset IDs," and `Preset` should stay pack-agnostic per "presets are standalone entities"). Remove `Preset.packId`. Update `server/repositories/PackRepository.ts`, `PresetRepository.ts`, `server/services/PackService.ts`, `PresetService.ts`, and the pack/preset route handlers accordingly. UI implication: a preset in two packs will now show under both pack tabs in `src/components/presets/Presets.tsx` — expected behavior, not a bug. (2) Entitlement: no purchase/entitlement system exists anywhere in the codebase today (Stripe is still research-only) — reuse the existing `isPremium` + `PremiumTrialModal` trial-gating mechanism as-is; the `@user` pack behaves exactly like the existing Galaxy/Sacred premium packs. Do not build new entitlement infra here. (3) Of the 5 existing Shlump preset configs in `src/config/presets.ts` (lines 32-36), copy 4 into the new entries verbatim, swapping the sprite reference to the `@user` sigil, by name: `shlump` → `@user`, `shlumpSpin` → `@userSpin`, `shlumpSalad` → `@userSalad`, `hyperShlump` → `Hyper@user`. `shlumpSkull` has no `@user` counterpart — leave it as-is in the Shlump pack (not migrated, not removed; its fate is covered by the Shlump-pack Decision item). (4) Update `scripts/seed-packs.js` to add the `@user` pack (premium), then, after `scripts/seed-presets.js` has run, populate every pack's `Pack.presetIds` by looking up preset IDs by name — make this step idempotent (re-running either script re-syncs membership rather than erroring or duplicating). `scripts/seed-presets.js` continues to write the legacy `pack` string field only; it does not populate `Pack.presetIds`. Document in the PR that `yarn seed` must run before `yarn seed-packs` for membership to populate correctly.

- [ ] #7 [stack: user-platform] Implement reserved particle reference `@user` (sigil syntax). Presets referencing `@user` resolve at load time to the current user's logo particle. Resolution order: current user's logo from per-user settings → user's display name rendered as text (Spotify profile name preferred, else Clerk name from other providers) → preset-defined fallback particle → app default. (stack order 5/7)
  - Context: presets like ShlumpSpin and BeatzMeSpin are identical except the particle is the artist's logo; this makes that customization automatic per user. Pre-existing presets must not change; `@user` is for new presets only.
  - Depends on the users collection task for logo and display name storage; if that task is blocked, build the resolver against the lower rungs (preset fallback, app default) behind the same interface.
  - Design decision: there's no user-facing "particle name" concept in the codebase today (uploads are anonymous `data:`/R2 URLs) — no naming system is being introduced here. `@user` is implemented as a literal sentinel string compared directly inside `sprites.value`, not a general namespace. The "disallow `@` in user-created particle names" idea from the original ask doesn't apply since particles aren't named entities; if a future task introduces user-chosen filenames (e.g. as part of R2 uploads), `@`-prefix validation should be added there, not here.
  - Sub-tasks: (1) New `src/utils/userReference.ts` resolver utility implementing the fallback chain, reading `logoParticle` from the users-collection task. `src/utils/presetSpriteSrc.ts` only accepts image-like values (`data:`, `blob:`, absolute URLs, public paths), so the display-name rung must not return raw text — render the display name to a sprite-compatible asset (e.g. a canvas/SVG text-to-image `data:` URL) before returning it, so every rung of the chain resolves to something `presetSpriteSrc.ts` and `sprites.value` can already handle unmodified. (2) Hook into config normalization in `ConfigProvider.tsx` (`normalizeLoadedPreset`) so `@user` resolves at load time, not save time, passing the resolved value through the existing `presetSpriteSrc`-compatible flow.
  - Acceptance: a preset referencing `@user` renders the logged-in user's logo; falls back to rendered display name when no logo is set; full fallback chain works signed out; existing presets render unchanged.
- [ ] #8 [stack: user-platform] Extend the `@user` resolver to display-name templating in preset and pack names: `@userSpin` displays as "BeatzMeSpin" for user BeatzMe, a pack named `@user` displays as "BeatzMe". Build as one utility with two consumers: particle references resolve to a renderable, name strings resolve to display text. (stack order 7/7)
  - Acceptance: preset and pack names containing `@user` render with the current user's display name everywhere names appear; falls back sensibly when signed out.
  - Sub-tasks: (1) Second consumer added to `src/utils/userReference.ts` from the resolver task above — extend, don't duplicate. (2) Wire into `ConfigProvider.tsx`'s `toLabel` (preset labels) and `src/components/presets/Presets.tsx`'s pack tab rendering (the `{pack}` display).
- [ ] #19 [stack: solo] Fix preset/pack visibility leak.
  - Acceptance: `PresetService.listPresetsForViewer` and `PackService.listPacks` return only built-in/seeded items plus the viewer's own; a signed-in user never sees another user's private presets/packs in list results; existing seeded/global visibility unchanged.
  - Sub-tasks: (1) `PresetRepository.findAll()` → filter to `Preset.find({ $or: [{ userId: { $exists: false } }, { userId: viewerId }] })` (built-in = absent `userId`, matching the existing unique-index comment). (2) `PackRepository.findAll()` → Packs have no null-userId state; all built-in packs are owned by the hardcoded `OWNER_ID` constant in `scripts/seed-packs.js`. Move that constant to a shared location (e.g. `server/constants.ts`) imported by both the seed script and the repository, and filter to `Pack.find({ $or: [{ userId: OWNER_ID }, { userId: viewerId }] })`. (3) If task #6's `Pack.presetIds` refactor has already landed, verify the filtered query still returns full membership correctly. (4) Note in the PR that this only fixes list-visibility — the "1 private pack" tier from task #11's monetization research is separate future quota enforcement, not in scope here.
  - NEEDS HUMAN: none.
- [ ] #20 [stack: solo] Premium-gate custom particle sprite upload.
  - Acceptance: uploading a custom particle sprite in `ParticleSpriteHud.tsx` requires premium entitlement, not just sign-in; non-premium signed-in users see the existing rainbow premium-outline treatment as a visual indicator.
  - Sub-tasks: (1) Thread `isPremium` (already available via pack/preset context types in `ConfigProvider.tsx`) into `ParticleSpriteHud.tsx`'s props. (2) Extend the `uploadDisabled` condition (line 72) to also require `isPremium`. (3) Apply the existing `.pack-tab--premium`/`.preset-item--premium` rainbow-outline CSS pattern from `Presets.css` to the upload control when locked. (4) Update sign-in-gated messaging (lines 114-119, 144-148, 223) to show a premium-gated message when signed in but not premium, keeping the sign-in message when signed out.
  - NEEDS HUMAN: confirm whether "premium" here means the same per-user `isPremium`/trial state used for packs (assumed, per the Discovered note referencing the existing rainbow treatment), or a separate feature-level entitlement.

## Verify (may already be done)

- [ ] #9 [stack: solo] Verify automated background removal and resize for particle uploads. "256x256 vs 512x512 target" was ambiguous — resolve and document the actual contract: the accepted source/input size, the stored/processed output size, and the size actually used at render time (they need not all match; e.g. a larger stored master with a smaller render-time thumbnail is fine, but say so explicitly). If done, record the confirmed dimensions per stage here and check off. If not, implement to one explicit target size per stage and document it here.

## Research (agent can draft findings, human decides)

- [ ] #10 [stack: solo] Research Spotify Web SDK integration scope: direct playback, user/artist track access, playlist access, top songs. Document auth flow, API limits, and premium-account requirements in `docs/research/spotify-sdk.md`. Do not implement yet.
- [ ] #11 [stack: solo] Draft Stripe integration plan: premium subscription plus per-pack purchases. Document proposed guest vs free vs premium tiers (e.g. free: 3 presets, 3 particle uploads, 1 private pack; ~$5/mo: 10 presets, 10 particles, 3 pub/private packs; ~$20/mo premium+/unlimited) in `docs/research/monetization.md`. Do not implement billing.

## Decisions (human only, do not attempt)

- [ ] #12 Decide fate of the original Shlump pack: remove, or keep as a specific-artist example. (Placement is decided: `@userSpin` free in Essentials, full `@user` pack premium.)
- [ ] #13 New visualization types as multiselect (new Three.js scene?). "Logo" viz type = audio-responsive 3D logo in center of screen, consuming the same `@user` resolver. Should "Video" split out as its own viz type (currently part of orbit/fractal/tunnel)?
- [ ] #14 Camera position macros (cross, circle, square, horizontal, vertical): timed vs audio/beat responsive? Double-click to add waypoints on pad or screen? (Pad is already click-to-interact.)
- [ ] #15 User acquisition strategy: IG account for Meta ads (Bassyndicate audience or promoter lookalike), target market definition (artists, VJs, music lovers) and how to reach them.

## Discovered

- CLAUDE.md's Commands section says "There is no `test` script yet" — a `test` script (`vitest run`) and 2 test files (14 passing tests) already exist. Found while verifying #16/#17. Update CLAUDE.md's Commands section to reflect this.
