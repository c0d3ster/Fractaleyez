# Completed 2026-08-01

- #1 [stack: user-platform] Move particle storage into R2 instead of the public directory. (stack order 1/7)
  PR: #144
  Notes: Public R2 bucket (default recommendation) with immutable Cache-Control on every object. Existing sprite references kept working unchanged — old `/public` paths and bare filenames still resolve via `presetSpriteSrc.ts`'s existing fallback branch; no DB migration of existing preset docs was needed. Added `server/services/StorageService.ts`, `scripts/migrate-particles-to-r2.js` (idempotent, uploads built-ins under `builtins/`), authenticated `POST /api/uploadParticle` (auth, format allowlist, byte-size cap, decoded-pixel-size cap via `image-size`, user-scoped key, no-overwrite), and rewired `ParticleSpriteHud.tsx`'s upload flow to use it instead of embedding base64 `data:` URLs. Offline/live-show handling: `src/utils/spriteCache.ts` warms the browser's Cache Storage API in the background and resolves synchronously in the visualizer's texture-loading path; verified with an automated no-network test rather than only documented. Added `vitest.config.ts` + first test files (repo had vitest/supertest/mongodb-memory-server as devDependencies but no config yet).
  NEEDS HUMAN: create the Cloudflare R2 bucket (public) + API token; add `R2_ACCOUNT_ID`/`R2_ACCESS_KEY_ID`/`R2_SECRET_ACCESS_KEY`/`R2_BUCKET_NAME`/`R2_PUBLIC_URL` to Vercel and local `.env`; then run `yarn migrate-particles-to-r2` once.
