# Completed 2026-09-20

- #4 [stack: user-platform] Add configuration gear for per-user settings, stored on the users collection.
  PR: #165
  Notes: Added `PATCH /api/me` (alongside existing `GET /api/me`) to persist `crossfadeDurationMs`, `logoParticle`, and `hud.enabledFreqBands` on the users collection. Made particle crossfade duration runtime-mutable so it's driven by the user setting instead of a fixed constant. Added a gear icon next to `UserButton` in `TopBar` (hidden when signed out) opening a panel with a crossfade slider and logo upload (reuses the `/api/uploadParticle` R2 endpoint, not the old inline `data:` URL path). `FrequencyHud`'s per-band toggle now persists through the same settings path. Settings auto-save on change, debounced 500ms (no explicit Save button, per the task's own "missing acceptance criteria" note). `logoParticle` is written/persisted here but not yet consumed by the visualization — that's the `@user` resolver task (#7). Stacked on #3 (PR #164, still open) — targets that branch, not main.
  NEEDS HUMAN: none. Note: no live-browser verification this session (`.env` inaccessible in the sandbox) — verified via typecheck, lint, unit tests, and a production build instead.
